import Foundation
import WebKit

// Phase 0 stub — implementation in Phase 5
// INVARIANT: Character offsets must use UTF-16 code unit counts, text nodes only.
// Must match EPUBParser.swift and PaginationJS.swift exactly.
class HighlightBridge {
    // Will serialize/deserialize highlight positions and inject highlight spans via WKWebView
}
