import Foundation
import AppKit

// Phase 0 stub — implementation in Phase 3/7
class ReaderPreferences: ObservableObject {
    // Will store user style preferences: font, size, line height, colors, max width
    // Changes here trigger a full HTML reload (invariant: never patch live DOM for style changes)
}
