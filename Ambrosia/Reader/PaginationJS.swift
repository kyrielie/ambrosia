import Foundation

// Phase 0 stub — implementation in Phase 4
// INVARIANT: Character offsets here must use UTF-16 code unit counts, text nodes only.
// Must match EPUBParser.swift and HighlightBridge.swift exactly.
enum PaginationJS {
    static let script: String = """
    // Pagination JS — stub, implementation in Phase 4
    """
}
