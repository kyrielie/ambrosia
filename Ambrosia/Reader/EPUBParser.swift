import Foundation

// Phase 0 stub — implementation in Phase 2
// INVARIANT: Character offsets are UTF-16 code unit counts, text nodes only, HTML tags excluded.
// This contract must match PaginationJS.swift and HighlightBridge.swift exactly.
class EPUBParser {
    // Will parse OPF, spine, and HTML content from an EPUB archive
}
